<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white flex items-center justify-center">
      <div class="max-w-md w-full">
        <h1 class="text-3xl font-bold mb-6">Login</h1>
        <form action="/login" method="post" class="flex flex-col space-y-4">
          <input
            class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md placeholder-gray-500"
            type="email"
            name="email"
            placeholder="Email"
          />
          <input
            class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md placeholder-gray-500"
            type="password"
            name="password"
            placeholder="Password"
          />
          <input
            class="px-4 py-2 bg-blue-500 hover:bg-blue-600 rounded-md cursor-pointer"
            type="submit"
            value="Login"
          />
        </form>
        <p class="mt-2">Do not have an account? <a href="/" class="text-blue-500">Create account</a></p>
      </div>
    </div>
  </body>
</html>
